# HH Monitor

Система мониторинга рынка труда на основе API hh.ru: сбор вакансий, живой дашборд,
финальные недельные отчёты. Конфигурируется под любую профессию — от поддержки до разработки.

## Архитектура

- `collector.py` — сбор вакансий (cron, ежедневно 8:00–20:00). Ходит в hh API по фразам
  роль-групп, сохраняет новые вакансии в SQLite, дотягивает полные карточки
  (зарплата, навыки, опыт, формат работы).
- `mark_archived.py` — каждый час помечает вакансии, исчезнувшие из поиска (закрыты).
- `app.py` + `templates/dashboard.html` — живой веб-дашборд (Flask + SQLite), обновление каждый час.
- `weekly_report.py` — финальный недельный отчёт (`weekly.html`, cron воскресенье 20:05)
  и снапшоты недели в таблице `weekly_reports` для сравнений.
- `.env` — ключи hh API (client_id/secret, User-Agent) и доступ к дашборду.

## Как выбирать роли, чтобы результат был релевантным

Фильтрация двухуровневая.

**Уровень 1 — поисковые фразы (ROLE_GROUPS в collector.py).**
hh ищет фразу по всему тексту описания, а не только в названии. Поэтому сырая выдача
всегда содержит шум: вакансия разработчика, где в тексте упомянут искомый термин,
придёт в выборку. Фразы в кавычках = точное вхождение. Роль-группы делят рынок на
сегменты (например, «руководители» и «тимлиды»).

**Уровень 2 — титульные маркеры.**
Чтобы убрать шум, в KPI попадают только вакансии, у которых в НАЗВАНИИ есть целевые
маркеры профессии/уровня. Три набора маркеров:
- положительные — роль остаётся в KPI (сегмент, который мониторим);
- отрицательные — роль исключается из KPI;
- смежные — роль видна в отдельном наблюдательном блоке, но не разбавляет KPI.

Рецепт настройки под любую профессию:
1. выписать 5–10 поисковых фраз роли в ROLE_GROUPS;
2. задать маркеры титулов (положительные/отрицательные/смежные);
3. посмотреть недельную выборку глазами и добить маркеры по реальным названиям.

Без уровня 2 медианы раздуваются зарплатами соседних сегментов
(кейс экземпляра: «медиана 250K» оказалась руководителями ИТ-инфраструктуры,
попавшими в выдачу по тексту описания).

## Дашборд: что означает каждый блок

- **Новых сегодня** — вакансии за текущий день; бейдж — динамика к вчера.
- **За неделю (с пн)** — накопление текущей недели; неделя всегда стартует с понедельника.
- **Медиана «от» / «до» (неделя)** — медианы нижней и верхней границ вилок в рублях.
  Считаются по разным подмножествам: вакансия может публиковать только одну границу.
  Прочерк = за неделю ещё не пришло вакансий с этой границей (норма для начала недели),
  бейдж при этом показывает ориентир прошлой недели.
- **Итоги недели (к прошлой неделе)** — три строки (число вакансий, медиана «от»,
  медиана «до») с двумя бейджами: «к прошлой неделе» (против полного снапшота) и
  «к этому дню прошлой» (против такого же окна прошлой недели) — честная динамика
  в начале недели, когда накопление ещё маленькое.
- **Количество вакансий за день** — столбчатая диаграмма за 7 дней: активность публикации.
  Выходные видны отдельно.
- **Медианная зарплата по дням** — кривые «от» и «до» за 7 дней; дни без данных
  заполняются предыдущим значением (ffill), чтобы кривая не рвалась.
- **Новые сегодня / Новые за неделю** — списки вакансий с фильтрами по зарплате
  (только с вилкой, 100k+, 150k+, 200k+).
- **Опыт и навыки** — структура спроса; клик по чипу фильтрует список вакансий.
- **Топ компаний за неделю** — кто нанимает активнее.
- **Архив закрытых за неделю** — сколько вакансий закрылось (скорость найма рынка).
- **Вакансии за выходные** — наблюдательный блок: публикации сб/вс.
- **Смежные роли** — соседний сегмент (настраивается маркерами): виден, но не входит в KPI.
- **Топ-10 зарплат недели** — верхняя граница рынка.

## Неделя и финальный отчёт

- Дашборд копит текущую неделю с понедельника в реальном времени.
- Финальный отчёт (`weekly.html`) — за последнюю завершённую неделю пн–вс
  (включая выходные). Генерация — воскресенье 20:05; страница держится до следующего
  воскресенья. Ручной запуск в любой день пересобирает ту же завершённую неделю —
  подглядывать в «незакрытую» неделю нельзя и не нужно.
- Снапшоты недели пишутся в `weekly_reports` и используются для сравнений «к прошлой неделе».

## Принципы честных цифр

- Медианы «от» и «до» — разные подмножества; прочерк — это отсутствие данных, а не ошибка.
- В начале недели выборка мала и медиана волатильна — оценивать рынок по закрытой неделе.
- Смежные сегменты держим вне KPI, иначе медиана чужая.
- Сбор распределён по дню, чтобы не ловить rate-limit hh API (понедельничный «взрыв»
  запросов после выходных приводил к бану токена на полдня).

## Конфигурация текущего экземпляра (пример)

Роль-группы: `head_of_support`, `team_lead_support`, `it_head` (мониторинг рынка
руководителей поддержки; смежный сегмент — ИТ-руководители). Маркеры и фразы —
в `collector.py` и в запросах `app.py`/`weekly_report.py`.

## Стек

Python 3, SQLite, Flask, nginx, cron, VPS. API: hh.ru (OAuth client_credentials).

## Метрики подробно (справочник)

### Верхние плитки
- **Новых сегодня** — число вакансий целевого сегмента, опубликованных за день
  (титульные фильтры + archived=0). Бейдж — % **к тому же дню прошлой недели**
  (вторник против вторника): дневная динамика без шума «к вчера».
- **За неделю (с пн)** — накопление вакансий сегмента с понедельника текущей недели.
- **Медиана «от» (неделя)** — медиана **нижних** границ опубликованных вилок за текущую
  неделю; считается по подмножеству вакансий, у которых есть граница «от».
  Прочерк = таких вакансий ещё не пришло (норма для начала недели), не ошибка.
- **Медиана «до» (неделя)** — медиана **верхних** границ; по своему подмножеству.

### Блок «Итоги недели» (к прошлой неделе)
Три строки: вакансии, медиана «от», медиана «до». У каждой два сравнения:
- цветной бейдж «X%» — к **полной** прошлой неделе (снапшот weekly_reports);
- бейдж «к тем же дням прошлой недели: X%» — к **срезу** прошлой недели
  «пн–та же дата», чтобы незакрытая текущая неделя честно сравнивалась с таким же
  незакрытым отрезком;
- серые бейджи при прочерке: «прошлая неделя: X ₽» (полная) и
  «прошлая неделя за те же дни: X ₽» (срез).

### Графики
- **Количество вакансий за день** — столбцы публикаций за 7 дней; выходные видны отдельно.
- **Медианная зарплата по дням** — кривые «от» и «до» за 7 дней; дни без данных
  заполняются предыдущим значением (ffill), кривая не рвётся.

### Списки и блоки
- **Новые сегодня / Новые за неделю** — списки с фильтрами по зарплате
  (с вилкой / 100k+ / 150k+ / 200k+).
- **Опыт и навыки** — структура спроса; клик по чипу фильтрует список вакансий.
- **Топ компаний за неделю** — работодатели по числу публикаций.
- **Архив закрытых за неделю** — число и % закрытых за неделю (скорость найма рынка).
- **Вакансии за выходные** — публикации сб–вс целевого сегмента.
- **Смежные роли** — соседний сегмент (ИТ-топы: CTO, CIO, ИТ-директора и т.п.):
  виден наблюдательно, в KPI не входит.
- **Топ-10 зарплат недели** — верхняя граница рынка.

### Недельный отчёт (weekly.html, маршрут /weekly)
- KPI недели: число вакансий, медианы «от»/«до», доля вилок 200k+,
  прозрачность вилок, спрос по опыту.
- Текстовая аналитика со сравнением к прошлой неделе (динамика спроса и медиан) —
  появляется, когда в weekly_reports есть снапшот прошлой недели.
- Блок «💰 Анализ «от–до»»: медианы, разрыв вилки в %, доля ролей с «от»/«до» ≥ 200k,
  вывод о достижимости целевого уровня.

## Changelog

### Неделя 24–25.08.2026 — фиксы методологии
- **Блокировка hh API**: замечено, что после выходных в 8 утра hh временно блокирует
  GET-запросы из-за всплеска запросов, накопившихся за выходные (бан токена на
  несколько часов). Решение: сбор распределён по дню (cron ежедневно 8:00–20:00)
  вместо единого понедельничного прогона.
- **Титульный фильтр**: в базу сохраняются только руководящие титулы;
  линейные специалисты не собираются.
- **Support-фильтр по титулу**: в KPI — только роли с маркерами поддержки в названии
  (поддержк, support, service desk, helpdesk, customer success, customer service,
  клиентского сервиса, itsm, сервис, service, care, мониторинг, 1/2 линии,
  L1/L2, Line 1/Line 2, сервиса поддержки). Кейс «медиана 250K»: ИТ-топы (CTO, CIO,
  директора по инфраструктуре) раздували цифру — вынесены в «Смежные роли».
- **Смежные роли**: блок показывает ИТ-топов включая архивные (hh архивирует роли
  через пару дней, и они пропадали); маркеры расширены до CTO/CIO/директоров.
- **Единая методология**: все счётчики, медианы и списки дашборда и недельной сводки
  считают одну выборку (титульные фильтры + archived=0).
- **Честные сравнения**: «Новых сегодня» — к тому же дню прошлой недели; подписи
  «к тем же дням прошлой недели» и «прошлая неделя за те же дни» вместо
  непонятного «к этому дню прошлой».
- **Анализ «от–до»** в недельной сводке: разрыв вилки, доля ролей ≥ 200k.
- **Отчёт в репозитории**: reports/weekly.html, маршрут /weekly; .env и *.db в .gitignore.

### Дополнение к changelog: новые поисковые фразы (точное покрытие)
Фразы в роль-группах коллектора (ROLE_GROUPS) расширены — только добавление, без удалений:
- **head_of_support**: «начальник отдела поддержки», «руководитель департамента поддержки»,
  «руководитель службы поддержки», «руководитель 1/2 линии», «руководитель мониторинга»,
  L1/L2 («руководитель поддержки L1/L2», «head of L1/L2»), Line 1/Line 2, first/second line,
  «руководитель сервиса поддержки», «head of service support», «service support manager»;
- **team_lead_support**: «тимлид 1/2 линии», «тимлид мониторинга», L1/L2,
  team lead/first/second line, «тимлид сервиса поддержки», «team lead service support»;
- **it_head**: без изменений (смежный сегмент).

Эффект: недельная выборка выросла с 28 до 39 ролей, медиана — с 132.5–175K до 150–182.5K.

### Changelog 01.09.2026 — стабильность и единый токен
- **DNS-сбой сервера** (29–30.08): systemd-resolved упал с ошибкой
  `Temporary failure in name resolution`, коллектор не собирал данные сутки.
  Починено: перезапуск сервиса + фолбэк на статические DNS 8.8.8.8/1.1.1.1 в /etc/resolv.conf.
- **«Лёгкий коллектор»** против банов hh API: токен кешируется в .token до истечения срока,
  пагинация останавливается на вакансиях старше 3 дней (~120 запросов за прогон вместо 1500+),
  крон раз в 2 часа (0 8-20/2).
- **Общий токен-менеджер (token_manager.py)**: collector.py и mark_archived.py делят
  один токен, hh больше не аннулирует его повторными OAuth-запросами (это была причина
  «тихой смерти» прогонов — 68 минут впустую на 401-ответы).
- **Fail-fast на 401**: коллектор проверяет токен одним тестовым запросом и падает
  за секунду вместо молотилки 68 минут при невалидности токена.
- **Первый полный недельный отчёт** (24–30.08): спрос −12.8% к прошлой неделе,
  медиана «до» +37.0% (250K), цель 200K+ достижима на верхней границе вилки.
- **Резюме под рынок**: в блок «Навыки» добавлены формулировки hh
  (Оптимизация бизнес-процессов, Управленческие навыки, Управление отношениями с клиентами),
  «Обо мне» обновлён под топ-навыки недели, желаемая зарплата 220 000 ₽.
